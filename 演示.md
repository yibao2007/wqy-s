<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    
</body>
<script>
    let a = 1;//这是一个元素
    let b = 2;//这也是一个元素
    let jihe1 { 1 , 2 }//1和2两个元素构成了集合1；1∈集合1 2∈集合1
    let jihe2 { a , b }//a = 1 , b = 2 ,此时，集合2与集合1的所有元素都相同，我们说集合1与集合2相等
    let jihe3 { 卖国俊 ， 周佳程 }//集合中的元素可以是任何东西，如坐标等，但 ①集合中不能有重复的元素
    // ②集合中的所有元素必须是确定的，并且 ③对元素的顺序没有要求
    //某学校中的所有游泳高手是一个集合（X），游泳高手的定义不明确，元素无法确定
    //如果说“某学校的所有游泳100M用时35秒的学生是一个集合”（√），此时定义明确，可以确定所有的元素
    //利用以上三种特征，我们可以解决一些数学问题
    （ 1 ） 设 a , b 是两个实数，集合A中含有 0 , b , b/a 三个元素，集合B中含有 1 , a , a + b 三个元素，且集合A与集合B相等，
    则 a + 2b = ?
    //条件：集合A与集合B相等
    //根据第16行的注释，集合A与集合B相等，我们可以得知集合A与集合B的三个元素都是分别对应相等的
    //因此，我们可以确定几种情况：① 集合B中的 a = 0 ; ②集合B中的 a + b 等于0
    //情况一中， 元素 a = 0，但在集合A中，有元素 a/b ，0 不可以做除数，因此，我们可以排除情况一，所以 a + b = 0
    //我们有可以根据集合B中的元素 1 得到两种情况：①集合A中，b = 1 ； ②集合A中，a/b = 1
    //如果集合A中 b = 1，那么我们可以由 a + b = 0 得到 a = -1；代入可以得知：
    集合A { 0 , 1 , -1 }// b = 1 , b/a = -1
    集合B { 1 , -1 , 0 }//集合的无序性,元素的顺序不影响集合相等 // a + b = 0 , a = -1 //此时，集合A与集合B相等，此题得解
    //我们再来看 ②集合A中， a/b = 1
    //此时 a = b，根据 ②集合中不能有重复的元素 我们可以确定：b/a = a + b (因为 a 等于 b 了，所以只能是 b/a = a + b)
    //但根据刚才的条件（a/b = 1 , a + b = 0），显然，这一个情况是矛盾的
    //最终我们可以确定：b = 1 , a = -1 // a + 2b = -1 + 2= 1

</script>
<script>
    （ 2 ）设集合A含有两个元素 x , y , B含有两个元素 0 , x² , 若 A = B, 则实数x = __ ; y = __.
    A { x , y };
    B { 0 , x² };
    //同理，根据两个集合相等，我们可以得到两种情况：① x = 0 , y = x² ② x = x² , y = 0
    //先来看情况一：∵ x = 0  ∴ y = x² = 0 = x
    //根据元素的互异性，x ≠ y ，因此，情况①是错误的
    //再来看情况二：x = x² ,即 x 的平方等于它本身，这样的数一共有两个：1 , 0
    //根据情况一：x ≠ 0 ，∴ x = 1 ; y = 0
    得到：A { 1 , 0 };
         B { 0 , 1 };
    //符合题意
</script>
<script>
    （ 3 ）若由 a , b/a , 1 组成的集合A与由 a² ，a + b ，0 组成的集合B相等，则 a^2022 + b^2022 的值为__.
    
</script>
</html>
